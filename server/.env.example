# ImmoPi Manager - Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=8000
NODE_ENV=production

# CORS Configuration (optional)
# CORS_ORIGIN=http://localhost:3000

# Authentication
# Use plain text password (simpler but less secure)
APP_PASSWORD=your_secure_password_here

# OR use bcrypt hashed password (more secure)
# Generate hash with: node auth-middleware.js hash your_password
# APP_PASSWORD_HASH=$2b$10$example_hash_here

# Google Gemini AI API
API_KEY=your_gemini_api_key_here

# Google Drive Backup Configuration
GOOGLE_DRIVE_FOLDER_ID=your_backup_folder_id_here
GOOGLE_CREDENTIALS_PATH=./google-credentials.json

# Google Drive Document Storage Configuration
# Create a separate folder for documents (different from backups)
GOOGLE_DRIVE_DOCUMENTS_FOLDER_ID=your_documents_folder_id_here

# Notes:
# - Get Gemini API key from: https://makersuite.google.com/app/apikey
# - Create Google Cloud service account and download credentials JSON
# - Create TWO folders in Google Drive:
#   1. "ImmoPi Backups" - for weekly database backups
#   2. "ImmoPi Documents" - for uploaded documents (organized by property/year/category)
# - Share BOTH folders with the service account email (write access)
# - Copy the folder IDs from the Drive URLs
# - Folder ID format: https://drive.google.com/drive/folders/FOLDER_ID_HERE
